---
import BaseLayout from '@/layouts/BaseLayout.astro'
import AdminHeader from '@/components/common/AdminHeader.tsx'
import AppSidebarLayout from '@/components/common/AppSidebarLayout.tsx'
import AdminFooter from '@/components/common/AdminFooter.tsx'
import ReportsPage from '@/components/laporan/ReportsPage.tsx'
import ProtectedDashboard from '@/components/auth/ProtectedDashboard.tsx'
---

<BaseLayout title="Laporan - SIM4LON">
  <ProtectedDashboard client:load>
    <AdminHeader client:load />
    <AppSidebarLayout client:load headerHeight="64px">
      <main class="flex-1 flex flex-col">
        <div class="flex-1 overflow-auto">
          <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
            {/* Page Header */}
            <div class="mb-6 animate-fadeInDown">
              <h1 class="text-3xl font-bold text-foreground">Laporan</h1>
              <p class="text-muted-foreground mt-2">Laporan penjualan, pembayaran, dan stok</p>
            </div>

            {/* Reports Content */}
            <div class="animate-fadeInUp">
              <ReportsPage client:load />
            </div>
          </div>
        </div>
        <AdminFooter />
      </main>
    </AppSidebarLayout>
  </ProtectedDashboard>
</BaseLayout>
